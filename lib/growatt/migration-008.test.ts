import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/008_growatt_daily_energy.sql","utf8");describe("008 Growatt napi energia migráció",()=>{it("egyedi user+dátum és nem negatív energia, redundáns user/date index nélkül",()=>{expect(sql).toMatch(/unique\s*\(user_id,\s*local_date\)/i);expect(sql).toMatch(/energy_kwh\s*>=\s*0/i);expect(sql).not.toContain("growatt_daily_energy_user_date_idx");expect(sql).toContain("growatt_daily_energy_date_idx")});it("RLS minden műveletnél saját userre korlátoz",()=>{expect(sql).toMatch(/enable row level security/i);for(const operation of["select","insert","update","delete"])expect(sql).toMatch(new RegExp(`for ${operation}`,"i"));expect((sql.match(/auth\.uid\(\)\s*=\s*user_id/g)??[]).length).toBeGreaterThanOrEqual(4)});it("updated_at triggert és kontrollált státuszt használ",()=>{expect(sql).toContain("set_updated_at()");expect(sql).toMatch(/quality_status in \('complete', 'provisional', 'missing', 'invalid'\)/)})});
