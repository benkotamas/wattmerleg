import {describe,expect,it,vi}from"vitest";vi.mock("server-only",()=>({}));
import {mapDevices,mapLatest,mapPlants,safeNumber}from"./mapper";import type{GrowattDeviceFieldMap,GrowattLatestFieldMap,GrowattPlantFieldMap}from"./types";
const map:GrowattPlantFieldMap&GrowattDeviceFieldMap&GrowattLatestFieldMap={plantList:"payload.plants",plant:{id:"pid",name:"label",timezone:"tz",status:"state"},deviceList:"payload.devices",device:{id:"did",serialNumber:"serial",type:"kind",model:"model",status:"state",plantId:"pid"},latest:{measuredAt:"data.at",currentPowerW:"data.power",todayEnergyKwh:"data.today",monthEnergyKwh:"data.month",yearEnergyKwh:"data.year",lifetimeEnergyKwh:"data.total",gridImportPowerW:"data.import",gridExportPowerW:"data.export",loadPowerW:"data.load",batterySocPercent:"data.soc"}};
describe("Growatt mapper",()=>{it("plant és device listát explicit mapping alapján olvas",()=>{expect(mapPlants({payload:{plants:[{pid:1,label:"Otthon"}]}},map)[0]).toMatchObject({id:"1",name:"Otthon",timezone:null});expect(mapDevices({payload:{devices:[{did:"d",serial:"ABC"}]}},map)[0]).toMatchObject({id:"d",serialNumber:"ABC",model:null})});it("numeric stringet számmá alakít, hiányzó opcionális mezőt nullon hagy",()=>{const latest=mapLatest({data:{at:"2026-08-02T12:00:00Z",power:"1234.5",today:"8.2",soc:null}},map,{plantId:"p",deviceId:"d",serialNumber:"s"});expect(latest.currentPowerW).toBe(1234.5);expect(latest.todayEnergyKwh).toBe(8.2);expect(latest.monthEnergyKwh).toBeNull();expect(latest.batterySocPercent).toBeNull();expect(latest.rawCapabilities).toEqual(["measuredAt","currentPowerW","todayEnergyKwh"])});it("null és hibás szám nem lesz nulla",()=>{expect(safeNumber(null)).toBeNull();expect(safeNumber("x")).toBeNull()});it("hibás lista response normalizált hibát ad",()=>expect(()=>mapPlants({},map)).toThrow("A Growatt API válasza"))});
