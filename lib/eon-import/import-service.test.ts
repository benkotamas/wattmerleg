import{describe,expect,it,vi}from"vitest";import*as XLSX from"xlsx";import{importEonWorkbook}from"./import-service";
const bytes=()=>{const rows=[["Dátum/Idő","+A","-A"],["MAXIMUM ÉRTÉK",1,2],["ÖSSZEG",1,2],["2026.08.04 00:00",1,2]],wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.aoa_to_sheet(rows),"Adatok");return XLSX.write(wb,{type:"buffer",bookType:"xlsx"})as Buffer};
describe("E.ON import service",()=>{it("returns the first import RPC counters",async()=>{const client={rpc:vi.fn().mockResolvedValue({data:{batch_id:"batch",inserted_rows:1,updated_rows:0,unchanged_rows:0},error:null})},result=await importEonWorkbook({userId:"owner",bytes:bytes(),source:"eon_portal_export",client,referenceDate:"2026-08-04"});expect(result).toMatchObject({batchId:"batch",insertedRows:1});expect(client.rpc).toHaveBeenCalled()});it("does not call RPC when the optional automatic policy rejects parsed data",async()=>{const client={rpc:vi.fn()};await expect(importEonWorkbook({userId:"owner",bytes:bytes(),source:"eon_portal_export",client,referenceDate:"2026-08-04",validateParsed:()=>{throw new Error("policy-rejected")}})).rejects.toThrow("policy-rejected");expect(client.rpc).not.toHaveBeenCalled()});it("manual import remains unchanged without an automatic policy",async()=>{const client={rpc:vi.fn().mockResolvedValue({data:{batch_id:"batch",inserted_rows:1,updated_rows:0,unchanged_rows:0},error:null})};await expect(importEonWorkbook({userId:"owner",bytes:bytes(),source:"eon_portal_export",client,referenceDate:"2026-08-04"})).resolves.toMatchObject({batchId:"batch"})});it("maps duplicate and database errors",async()=>{for(const [message,code]of[["EON_ALREADY_IMPORTED","EON_ALREADY_IMPORTED"],["private database detail","EON_DATABASE_ERROR"]]){const client={rpc:vi.fn().mockResolvedValue({data:null,error:{message}})};await expect(importEonWorkbook({userId:"owner",bytes:bytes(),source:"eon_portal_export",client,referenceDate:"2026-08-04"})).rejects.toMatchObject({code})}});it("does not call database on hash mismatch",async()=>{const client={rpc:vi.fn()};await expect(importEonWorkbook({userId:"owner",bytes:bytes(),source:"eon_portal_export",client,expectedSha256:"0".repeat(64),referenceDate:"2026-08-04"})).rejects.toMatchObject({code:"EON_PREVIEW_HASH_MISMATCH"});expect(client.rpc).not.toHaveBeenCalled()})});
