import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";import{eonOwnerId}from"./config";
describe("Gmail integration security contract",()=>{it("prefers dedicated owner and retains manual fallback",()=>{expect(eonOwnerId({NODE_ENV:"test",EON_ALLOWED_USER_ID:"eon",GROWATT_ALLOWED_USER_ID:"growatt"} as NodeJS.ProcessEnv)).toBe("eon");expect(eonOwnerId({NODE_ENV:"test",GROWATT_ALLOWED_USER_ID:"growatt"} as NodeJS.ProcessEnv)).toBe("growatt")});it("does not expose secrets in status DTO source",()=>{const source=readFileSync("app/api/eon/gmail/status/route.ts","utf8");expect(source).not.toMatch(/refreshToken|clientSecret|emailAddress/)});it("cron route is no-store and has bounded duration",()=>{const source=readFileSync("app/api/cron/eon-gmail/route.ts","utf8");expect(source).toContain("maxDuration=60");expect(source).toContain("gmailNoStore")})});
