import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/012_eon_gmail_ingestion.sql","utf8");
describe("migration 012",()=>{it("has ownership uniqueness and RLS",()=>{expect(sql).toContain("unique(user_id,gmail_message_id)");expect(sql).toContain("enable row level security");expect(sql).toContain("auth.uid()=user_id")});it("allows only service role mutations",()=>{expect(sql).toContain("revoke all on public.eon_gmail_messages from anon,authenticated");expect(sql).toContain("grant execute");expect(sql).toContain("to service_role")});it("uses claim lease, token and bounded attempts",()=>{expect(sql).toContain("interval '5 minutes'");expect(sql).toContain("claim_token");expect(sql).toContain("attempt_count<5")});it("prevents invalid finish transitions",()=>expect(sql).toContain("EON_GMAIL_INVALID_TRANSITION"))});
