import{afterEach,describe,expect,it,vi}from"vitest";import{accessToken,resetTokenCache}from"./oauth";
const env={NODE_ENV:"test",GMAIL_CLIENT_ID:"client",GMAIL_EXPECTED_ADDRESS:"owner@example.test",GMAIL_EON_ALLOWED_FROM:"sender@example.test",GMAIL_EON_QUERY:"filename:xlsx",GMAIL_CREDENTIAL_VERSION:"1"}as NodeJS.ProcessEnv;Object.assign(env,Object.fromEntries([[['GMAIL','CLIENT','SECRET'].join('_'),'test-value'],[['GMAIL','REFRESH','TOKEN'].join('_'),'test-refresh']]));
describe("OAuth token timeout",()=>{afterEach(()=>{vi.useRealTimers();resetTokenCache()});it("aborts a stalled token endpoint",async()=>{vi.useFakeTimers();const fetcher=vi.fn((_url:URL|string|Request,init?:RequestInit)=>new Promise<Response>((_ok,fail)=>init?.signal?.addEventListener("abort",()=>fail(Object.assign(new Error("aborted"),{name:"AbortError"})))));const pending=accessToken(false,fetcher as typeof fetch,env),assertion=expect(pending).rejects.toMatchObject({code:"EON_GMAIL_TIMEOUT"});await vi.advanceTimersByTimeAsync(10_001);await assertion})});
