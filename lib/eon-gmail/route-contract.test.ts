import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";
describe("Gmail route contracts",()=>{const cron=readFileSync("app/api/cron/eon-gmail/route.ts","utf8"),sync=readFileSync("app/api/eon/gmail/sync/route.ts","utf8"),status=readFileSync("app/api/eon/gmail/status/route.ts","utf8");it("cron is bounded, secret protected and fail-closed",()=>{expect(cron).toContain("maxDuration=60");expect(cron).toContain("cronAuthorized");expect(cron).toContain("EON_GMAIL_NOT_CONFIGURED")});it("manual sync uses authenticated owner context and server-owned user id",()=>{expect(sync).toContain("eonImportContext");expect(sync).toContain("auth.userId");expect(sync).not.toContain("request.json")});it("status uses full database aggregation and an error allowlist",()=>{expect(status).toContain("get_eon_gmail_status");expect(status).toContain("SAFE_CODES");expect(status).not.toMatch(/gmail_message_id|claim_token|attachment_sha256/)})});
