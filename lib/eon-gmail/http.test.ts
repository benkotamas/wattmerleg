import{describe,expect,it}from"vitest";import{cronAuthorized,gmailFail}from"./http";import{EonGmailError}from"./errors";
const good="a".repeat(32);describe("Gmail route safety",()=>{it.each(["","YOUR_CRON_SECRET","YOUR_ANYTHING","[SENSITIVE]","short","a".repeat(32)+"\n"])("rejects invalid cron secret %j",secret=>expect(cronAuthorized(new Request("http://x",{headers:{authorization:`Bearer ${secret}`}}),secret)).toBe(false));it("accepts only the exact long Bearer secret",()=>{expect(cronAuthorized(new Request("http://x",{headers:{authorization:`Bearer ${good}`}}),good)).toBe(true);expect(cronAuthorized(new Request("http://x",{headers:{authorization:`Bearer ${"b".repeat(32)}`}}),good)).toBe(false)});it("returns controlled not-configured 503 and no-store",async()=>{const r=gmailFail(new EonGmailError("EON_GMAIL_NOT_CONFIGURED",503));expect(r.status).toBe(503);expect(r.headers.get("cache-control")).toContain("no-store");expect(await r.json()).toEqual({error:{code:"EON_GMAIL_NOT_CONFIGURED"}})})});
