import{NextResponse}from"next/server";import{eonImportContext}from"@/lib/eon-import/route-auth";import{accessFail,fail,noStore}from"@/lib/eon-import/http";import{EonImportError}from"@/lib/eon-import/errors";export const runtime="nodejs";export async function GET(){const auth=await eonImportContext();if(auth.access!=="allowed")return accessFail(auth.access);const{data,error}=await auth.client.from("eon_import_batches").select("id,period_start,period_end,status,inserted_rows,updated_rows,unchanged_rows,complete_days,provisional_days,incomplete_days,warning_codes,created_at").eq("user_id",auth.userId).order("created_at",{ascending:false}).limit(20);return error?fail(new EonImportError("EON_DATABASE_ERROR",503)):NextResponse.json({imports:data??[]},{headers:noStore})}
