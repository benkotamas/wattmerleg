"use client";

import { useState } from "react";
import { AppShell } from "@/components/app-shell";
import { formatKwh } from "@/components/format";
import { PageState } from "@/components/page-state";
import { PeriodList } from "@/components/period-list";
import { useEnergyData } from "@/lib/data";
import { monthlyStatistics } from "@/lib/statistics";
import { heatingSeasonStatistics, seasonalAnnualForecast } from "@/lib/seasonal-forecast";
import { Bar, BarChart, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

type ChartView = "combined" | "consumption" | "production" | "balance";
const views: { id: ChartView; label: string }[] = [
  { id: "combined", label: "Fogyasztás + termelés" }, { id: "consumption", label: "Fogyasztás" },
  { id: "production", label: "Termelés" }, { id: "balance", label: "Egyenleg" },
];

export default function StatisticsPage() {
  const { period, readings, allReadings, periods, tariff, loading, error } = useEnergyData();
  const [view, setView] = useState<ChartView>("combined");
  if (loading || error) return <AppShell><PageState loading={loading} error={error}/></AppShell>;
  const stats = monthlyStatistics(readings);
  const yearly = stats.reduce((sum, row) => ({ consumption: sum.consumption + row.consumption, production: sum.production + row.production }), { consumption: 0, production: 0 });
  const seasonal = period ? seasonalAnnualForecast(period, readings, allReadings, tariff) : null;
  const latest = allReadings.at(-1);
  const heating = seasonal && latest ? heatingSeasonStatistics(allReadings, new Date(latest.reading_at), tariff) : null;
  return (
    <AppShell>
      <h1 className="text-3xl font-black">Statisztika</h1><p className="mt-1 text-sm text-slate-500">A mérési időközök havi, időarányos felosztása</p>
      <div className="mt-5 grid grid-cols-2 gap-3"><StatCard label="Időszaki fogyasztás" value={formatKwh(yearly.consumption)}/><StatCard label="Időszaki termelés" value={formatKwh(yearly.production)}/></div>
      <section className="card mt-3 overflow-hidden p-3 sm:p-5">
        <h2 className="font-black">Havi energia (kWh)</h2>
        <div className="mt-3 flex gap-2 overflow-x-auto pb-2">
          {views.map(item => <button key={item.id} onClick={() => setView(item.id)} className={`shrink-0 rounded-full px-3 py-2 text-xs font-bold ${view === item.id ? "bg-emerald-700 text-white" : "bg-slate-100 text-slate-600"}`}>{item.label}</button>)}
        </div>
        <div className="mt-2 h-72 min-w-0 w-full">{stats.length ? <ResponsiveContainer width="100%" height="100%"><BarChart data={stats} margin={{ left: -24, right: 0, bottom: 4 }}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="shortLabel" fontSize={10} interval="preserveStartEnd" minTickGap={22}/><YAxis fontSize={10} width={48}/><Tooltip formatter={(value) => `${Number(value).toFixed(1)} kWh`} labelFormatter={(_, payload) => payload[0]?.payload.label ?? ""}/>{view === "combined" && <Legend/>}{(view === "combined" || view === "consumption") && <Bar dataKey="consumption" name="Fogyasztás" fill="#f97316" radius={[5,5,0,0]}/>} {(view === "combined" || view === "production") && <Bar dataKey="production" name="Termelés" fill="#168447" radius={[5,5,0,0]}/>} {view === "balance" && <Bar dataKey="balance" name="Egyenleg" fill="#2563eb" radius={[5,5,0,0]}/>}</BarChart></ResponsiveContainer> : <div className="grid h-full place-items-center text-sm text-slate-500">A grafikonhoz legalább két mérés szükséges.</div>}</div>
      </section>
      <div className="mt-3 space-y-2">{[...stats].reverse().map(row => <article key={row.month} className="card p-4"><div className="flex flex-wrap items-center justify-between gap-2"><p className="font-black capitalize">{row.label}</p><div className="flex flex-wrap gap-1">{row.estimated && <span className="rounded-full bg-amber-100 px-2 py-1 text-xs font-bold text-amber-800">Becsült, időarányos</span>}{row.hasDataWarning && <span className="rounded-full bg-red-50 px-2 py-1 text-xs font-bold text-red-700">Hibás mérődeltát kihagyva</span>}</div></div><div className="mt-3 grid grid-cols-3 gap-2 text-sm"><Value label="Fogyasztás" value={formatKwh(row.consumption)} color="text-orange-700"/><Value label="Termelés" value={formatKwh(row.production)} color="text-emerald-700"/><Value label="Egyenleg" value={formatKwh(row.balance)} color="text-blue-700"/></div></article>)}</div>
      {seasonal && <section className="card mt-6 overflow-hidden p-3 sm:p-5"><div className="flex flex-wrap items-center justify-between gap-2"><div><p className="text-xs font-bold text-emerald-700">SZEZONÁLIS FORECAST</p><h2 className="text-xl font-black">Tényleges és várható havi fogyasztás</h2></div><span className="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold text-slate-600">{seasonal.confidence === "high" ? "Magas" : seasonal.confidence === "medium" ? "Közepes" : "Alacsony"} megbízhatóság</span></div><p className="mt-1 text-xs text-slate-500">Korábbi évek azonos hónapjainak fogyasztási mintája alapján.</p><div className="mt-4 h-72 w-full min-w-0"><ResponsiveContainer width="100%" height="100%"><BarChart data={seasonal.months} margin={{ left: -24, right: 0 }}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="shortLabel" fontSize={10} interval="preserveStartEnd" minTickGap={24}/><YAxis fontSize={10} width={48}/><Tooltip formatter={(value) => `${Number(value).toFixed(1)} kWh`} labelFormatter={(_, payload) => payload[0]?.payload.label ?? ""}/><Legend/><Bar dataKey="actualConsumption" name="Tényleges" stackId="consumption" fill="#f97316" radius={[4,4,0,0]}/><Bar dataKey="expectedConsumption" name="Becsült" stackId="consumption" fill="#fdba74" stroke="#ea580c" strokeDasharray="4 3" radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></div></section>}
      {heating && <section className="card mt-4 p-4 sm:p-5"><div className="flex flex-wrap items-center justify-between gap-2"><div><p className="text-xs font-bold text-orange-700">FŰTÉSI SZEZON</p><h2 className="text-xl font-black">{heating.range.label} {heating.range.active ? "aktuális szezon" : "következő szezon"}</h2></div><span className={`rounded-full px-2 py-1 text-xs font-bold ${heating.range.active ? "bg-orange-100 text-orange-800" : "bg-blue-100 text-blue-800"}`}>{heating.range.active ? "Folyamatban" : "Előrejelzés"}</span></div><p className="mt-1 text-sm text-slate-500">{new Intl.DateTimeFormat("hu-HU", { dateStyle: "long" }).format(heating.range.start)} – {new Intl.DateTimeFormat("hu-HU", { dateStyle: "long" }).format(heating.range.end)}</p><div className="mt-5 grid grid-cols-2 gap-4 sm:grid-cols-4"><HeatingValue label="Eddigi fogyasztás" value={formatKwh(heating.actualConsumption)}/><HeatingValue label="Napi átlag" value={formatKwh(heating.dailyAverage)}/><HeatingValue label="Havi átlag" value={formatKwh(heating.monthlyAverage)}/><HeatingValue label="Szezonális előrejelzés" value={formatKwh(heating.expectedTotal)}/><HeatingValue label="Historikus szezonátlag" value={heating.historicalAverage === null ? "Nincs adat" : formatKwh(heating.historicalAverage)}/><HeatingValue label="Forecast megbízhatóság" value={heating.forecastConfidence === "high" ? "Magas" : heating.forecastConfidence === "medium" ? "Közepes" : "Alacsony"}/><HeatingValue label="Előző szezon összesen" value={heating.previousTotal === null ? "Nincs adat" : formatKwh(heating.previousTotal)}/><HeatingValue label="Előző szezon azonos pontjához" value={heating.changeAtSamePointPercent === null ? "Nincs adat" : `${heating.changeAtSamePointPercent > 0 ? "+" : ""}${heating.changeAtSamePointPercent.toLocaleString("hu-HU", { maximumFractionDigits: 1 })}%`}/></div><details className="mt-5 rounded-xl bg-slate-50 p-3"><summary className="cursor-pointer font-bold">Fűtési szezon havi forecastja</summary><div className="mt-3 space-y-2">{heating.months.map(month => <div key={month.month} className="flex flex-wrap items-center justify-between gap-2 border-b border-slate-200 pb-2 text-sm last:border-0"><span className="font-bold capitalize">{month.label}</span><span className="text-right tabular-nums">{formatKwh((month.actualConsumption ?? 0) + (month.expectedConsumption ?? 0))} · {month.expectedConsumption === null ? "tényleges" : month.actualConsumption ? "tényleges + becsült" : "becsült"}</span></div>)}</div></details>{heating.history.length > 0 && <div className="mt-5 border-t border-slate-100 pt-4"><h3 className="font-black">Fűtési szezonok összehasonlítása</h3><div className="mt-2 space-y-2">{heating.history.map(item => <div key={item.label} className="flex items-center justify-between rounded-xl bg-slate-50 p-3 text-sm"><span className="font-bold">{item.label} fűtési szezon</span><span className="tabular-nums">{formatKwh(item.consumption)}</span></div>)}<div className="flex items-center justify-between rounded-xl bg-orange-50 p-3 text-sm"><span className="font-bold">{heating.range.label} {heating.range.active ? "aktuális szezon" : "szezonális forecast"}</span><span className="text-right tabular-nums">{heating.range.active ? `eddig ${formatKwh(heating.actualConsumption)} · ` : ""}várható {formatKwh(heating.expectedTotal)}</span></div></div></div>}</section>}
      <PeriodList periods={periods} readings={allReadings} tariff={tariff}/>
    </AppShell>
  );
}

function StatCard({ label, value }: { label: string; value: string }) { return <div className="card min-w-0 p-4"><p className="text-xs font-bold text-slate-500">{label}</p><p className="mt-2 break-words text-xl font-black tabular-nums">{value}</p></div>; }
function Value({ label, value, color }: { label: string; value: string; color: string }) { return <div className="min-w-0"><p className="text-[11px] text-slate-500">{label}</p><p className={`mt-1 break-words font-bold tabular-nums ${color}`}>{value}</p></div>; }
function HeatingValue({ label, value }: { label: string; value: string }) { return <div className="min-w-0"><p className="text-xs text-slate-500">{label}</p><p className="mt-1 break-words font-black tabular-nums">{value}</p></div>; }
