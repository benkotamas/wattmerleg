import React from"react";import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{GrowattHistoricalSyncCard}from"./historical-sync-card";
describe("Growatt tartós backfill UI",()=>{it("az öt tartományválasztást és külön kezdő hónapot mutatja",()=>{const html=renderToStaticMarkup(<GrowattHistoricalSyncCard/>);for(const text of["Aktuális hónap","Előző lezárt hónap","Egyéni hónap","Hiányos hónapok javítása","Teljes előzmény","Teljes előzmény kezdő hónapja"])expect(html).toContain(text)});it("nem jelenít meg 28 napos felhasználói korlátot",()=>{const html=renderToStaticMarkup(<GrowattHistoricalSyncCard/>);expect(html).not.toContain("maximum 28 nap");expect(html).toContain("legfeljebb 7 napos Growatt blokkokkal")})});
